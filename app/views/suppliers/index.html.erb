<div class="app-section">
  <h2>Fournisseurs & base EAN</h2>
  <p class="text-muted">
    Gérer les fichiers Excel/CSV par marque. Ces données alimentent le scan d'inventaire.
  </p>

  <p style="margin-top: 16px;">
    <%= link_to "Ajouter un fournisseur", new_supplier_path, class: "btn-supplier" %>
  </p>
</div>

<div class="app-section">
  <% if @suppliers.any? %>
    <div class="card-grid">
      <% @suppliers.each do |supplier| %>
        <div class="card">
          <div>
            <h3><%= supplier.name %></h3>
            <p class="text-muted">Code interne : <%= supplier.code %></p>
            <p class="text-muted">EAN connus : <%= supplier.supplier_items.count %></p>
          </div>

          <div class="card-footer">
            <%= link_to supplier_path(supplier), class: "btn-supplier" do %>
              <svg class="icon-import" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
                <path d="M5 20h14v-2H5v2zm7-15v8.17l3.59-3.58L17 11l-5 5-5-5 1.41-1.41L11 13.17V5h1z"/>
              </svg>
              Télécharger fichier tarif
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-muted">Aucun fournisseur pour le moment.</p>
  <% end %>
</div>
